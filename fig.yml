
database:
  build: database
  privileged: true

phpmyadmin:
  build: phpmyadmin
  privileged: true
  ports:
  - "9000:80"
  links:
  - database

web:
  build: web
  privileged: true
  links:
  - database
  ports:
  - "8000:8000"
  volumes:
  - "web:/web"
  command: "scripts/run/dev.sh"
