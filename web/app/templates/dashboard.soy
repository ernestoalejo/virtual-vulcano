
{namespace vv}


/**
 * Dashboard page.
 @param plugins Plugins list.
 */
{template .dashboard}
  <div class="dashboard-grid">
    {foreach $plugin in $plugins}
      <div class="pull-left">
        <a href="{$plugin.url}">
          <i class="fa {$plugin.icon}"></i>

          {$plugin.name}
        </a>
      </div>
    {/foreach}
  </div>
{/template}

